<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.engineering.software.thewatch.userinterface.start.LoginActivity"
    android:background="@color/baseBackground"
    >

    <ImageView
        android:layout_width="match_parent"
        app:srcCompat="@mipmap/logo"
        android:id="@+id/logo"
        android:scaleType="centerCrop"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_height="200dp"
        android:adjustViewBounds="false"
        android:cropToPadding="false"
        android:layout_marginTop="22dp" />

    <TextView
        android:text="Privacy Policy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="11dp"
        android:id="@+id/btnPrivacyPolicy"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textColor="#1d1d1d"
        android:textSize="15sp" />

    <Button
        android:text="Sign in with Google"
        android:layout_marginBottom="14dp"
        android:drawableLeft="@drawable/google_48"
        android:background="@drawable/rounded_rectangle"
        android:layout_width="250dp"
        android:typeface="sans"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnTermsAndCondition"
        android:layout_centerHorizontal="true"
        android:elevation="0dp"
        android:textColor="#1d1d1d"
        android:padding="4dp"
        android:onClick="signIn"
        android:id="@+id/btnLogin" />

    <TextView
        android:text="Terms and Conditions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnTermsAndCondition"
        tools:textStyle="bold"
        android:textColor="#1d1d1d"
        android:textSize="15sp"
        android:layout_above="@+id/btnPrivacyPolicy"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5dp"
        android:layout_marginTop="10dp" />

</RelativeLayout>
